
---

# 1️⃣ Two Sum (LeetCode 1)

## Two_pointer only if it is sorted.

### Problem

Given an array `nums` and an integer `target`, return **indices of two numbers** such that:

```
nums[i] + nums[j] == target
```

You may assume **exactly one solution exists**, and you **cannot use the same element twice**.

---

## 2️⃣ Brute Force Approach

### 💡 Idea

* Try **all pairs**
* Check if their sum equals `target`

---

### ✅ Code (Brute Force)

```java
class Solution {
    public int[] twoSum(int[] nums, int target) {
        int n = nums.length;

        for (int i = 0; i < n; i++) {
            for (int j = i + 1; j < n; j++) {
                if (nums[i] + nums[j] == target) {
                    return new int[]{i, j};
                }
            }
        }
        return new int[]{};
    }
}
```

---

### ⏱ Time Complexity

```
O(n²)
```

### 💾 Space Complexity

```
O(1)
```

❌ Too slow for large inputs

---

## 3️⃣ Better Approach (Sorting + Two Pointers)

⚠ **Only valid if returning values or original indices are preserved separately**

---

### 💡 Idea

1. Store `(value, index)`
2. Sort by value
3. Use two pointers from both ends

---

### ✅ Code

```java
class Solution {
    public int[] twoSum(int[] nums, int target) {
        int n = nums.length;
        int[][] arr = new int[n][2];

        for (int i = 0; i < n; i++) {
            arr[i][0] = nums[i];
            arr[i][1] = i;
        }

        Arrays.sort(arr, (a, b) -> a[0] - b[0]);

        int left = 0, right = n - 1;

        while (left < right) {
            int sum = arr[left][0] + arr[right][0];
            if (sum == target) {
                return new int[]{arr[left][1], arr[right][1]};
            } else if (sum < target) {
                left++;
            } else {
                right--;
            }
        }
        return new int[]{};
    }
}
```

---

### ⏱ Time Complexity

```
O(n log n)
```

### 💾 Space Complexity

```
O(n)
```

---

## 4️⃣ Optimal Approach (HashMap) ✅

### 💡 Idea

* Store numbers as you iterate
* For each number:

  * Check if `target - num` exists in map
  * If yes → solution found

---

### ✅ Code (Best Solution)

```java
class Solution {
    public int[] twoSum(int[] nums, int target) {
        Map<Integer, Integer> map = new HashMap<>();

        for (int i = 0; i < nums.length; i++) {
            int complement = target - nums[i];

            if (map.containsKey(complement)) {
                return new int[]{map.get(complement), i};
            }
            map.put(nums[i], i);
        }
        return new int[]{};
    }
}
```

---

### ⏱ Time Complexity

```
O(n)
```

### 💾 Space Complexity

```
O(n)
```

---

## 5️⃣ Comparison Table

| Approach            | Time       | Space    | Notes                |
| ------------------- | ---------- | -------- | -------------------- |
| Brute Force         | O(n²)      | O(1)     | ❌                    |
| Sorting + 2 Pointer | O(n log n) | O(n)     | Needs index handling |
| HashMap             | **O(n)**   | **O(n)** | ✅ Best               |

---

## 6️⃣ Interview One-Liners 🔥

* **Brute Force:**

  > “We check all pairs, resulting in O(n²) time.”

* **HashMap (Optimal):**

  > “We use a HashMap to store visited numbers and check complements in O(1), achieving O(n) time.”

---

## 7️⃣ Common Interview Traps ⚠

❌ Using two pointers without sorting
❌ Forgetting original indices
❌ Using same element twice
❌ Not explaining space trade-off

---
